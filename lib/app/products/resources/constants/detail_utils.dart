import 'package:flutter/material.dart';

const EdgeInsets detailMarginWidgets = EdgeInsets.only(top: 10),
    detailPadding = EdgeInsets.symmetric(horizontal: 20);

const Key gridKey = Key('GRID'), listKey = Key('LIST');
